*** Variables ***
${sign_up.fullname}              css=input[placeholder="Full Name"]
${sign_up.mobile}               css=input[placeholder="0XXXXXXXXX"]
${sign_up.email}               css=input[type="email"][inputmode="email"]
${sign_up.password}            css=div[class="relative mb-1"] > input[type="password"]
${sign_up.confirm_password}     css=div[class="relative"] > input[type="password"]
${sign_up.create_account_button}      css=#btn-signup